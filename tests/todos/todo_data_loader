parse the files of a directory
get right file names using  a regex 
get data from file names 
remove first col
turn data from 512 to 256 Hz
combine last two colums
ability to select channels
count data structure
init of class taking participant and channels
pass sample every 256 timers
pass channels as diodes not numbers

	multithread pulling of samples
	fix that pytest slows it down
	
	pull_sample at a 256 pull_sample interval will always produce a array of 256 extra because pull_sample is pendng while time is alread over

	
	



